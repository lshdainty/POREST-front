import { cn } from "@/lib/utils"

export default function User() {
  return (
    <div
      className={cn(
        "flex w-full h-full p-[10px]" 
      )}
    >
      <span>user</span>
    </div>
  );
}