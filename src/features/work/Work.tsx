import { cn } from "@/lib/utils"

export default function Work() {
  return (
    <div
      className={cn(
        "flex w-full h-full p-[10px]" 
      )}
    >
      <h1>work page</h1>
    </div>
  );
}